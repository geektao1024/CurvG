# CurvG 项目简报

更新时间：2026-07-22

## 基本信息

| 项目项     | 值                                                 |
| ---------- | -------------------------------------------------- |
| 产品名     | CurvG                                              |
| 域名       | `https://curvg.com`                                |
| 首页 Title | `CurvG — Math Curve Animation Gallery & Generator` |
| 默认语言   | 英文，支持中文路由 `/zh`                           |
| 产品阶段   | 项目初始化 / 早期访问                              |
| 部署方向   | Cloudflare Workers + D1 + R2 + Sandbox             |

## 产品判断

CurvG 不应该被定义成普通的“文本生成视频”工具。更合理的产品核心是：

1. 公式和数学结构是事实来源。
2. AI 负责把数学意图拆成场景计划和可执行代码。
3. 用户可以检查公式、分镜和代码。
4. 渲染在隔离环境执行，并保留可复现的输入与产物。
5. 公开画廊负责内容发现、模板复用和 SEO。

## 目标用户

- 需要制作数学短视频的内容创作者。
- 需要展示函数、参数曲线和几何过程的教师。
- 希望快速生成 Manim 场景但仍需要检查数学正确性的开发者。
- 需要可复用数学动画模板的教育产品团队。

## 核心用户路径

### 画廊路径

`首页 → 浏览曲线 → 查看公式与参数 → 复用模板 → 创建自己的版本`

### 生成路径（计划）

`输入公式/意图 → 结构化解析 → 场景计划 → 代码检查 → 沙箱渲染 → 预览 → 导出`

## 本次初始化已完成

- CurvG 品牌名、Logo、色板与双语首页。
- 精确首页 Title 和 description。
- 由公式采样生成的 SVG 曲线预览，不依赖图片素材。
- Hero、曲线画廊、目标工作流、FAQ、早期访问 CTA、页脚。
- 隐藏未验证的模板定价，价格页明确说明尚未发布套餐。
- 本地 SQLite 开发环境与 Cloudflare D1 部署模板的品牌配置。
- 项目架构、页面规格和路线图文档。

## 尚未完成

- AI 公式解析和场景生成 API。
- Manim 代码生成、校验与版本管理。
- Cloudflare Sandbox 渲染任务。
- D1 自定义业务表和任务状态机。
- R2 渲染产物上传、签名访问和生命周期规则。
- 可搜索的动态画廊、作品详情页和编辑器。
- 真实定价与积分成本模型。

## 产品边界

- 不承诺 AI 输出绝对数学正确。
- 不在 Workers 进程内直接执行用户或模型生成的代码。
- 不把视频二进制存入 D1。
- 没有真实渲染成本数据前不发布套餐价格。
- 竞品截图只作为研究资料，不复制其品牌、文案或受保护素材。

## 发布前提醒

当前法律页面已按 CurvG 的产品类型做基础改写，但仍是工程初始化文本，不等同于法律审查。正式收费、处理未成年人数据或开放公开作品上传前，需要根据实际运营主体、地区和数据流重新审阅。
